public class main {

    App test = new App();
    //call convert on inputfile
    
    //then simulate a lost follower
    //then call search to check and if the size is less, return the message, the lost followers and the new count
}
